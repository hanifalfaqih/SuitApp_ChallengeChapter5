<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.allana.suitapp_challengechapter5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SuitApp_ChallengeChapter5">
        <activity
            android:name=".presentation.game.GameActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.menu.MainMenuActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.intro.IntroActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.splashscreen.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>